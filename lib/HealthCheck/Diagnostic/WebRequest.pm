package HealthCheck::Diagnostic::WebRequest;

# ABSTRACT: Make HTTP/HTTPS requests to web servers to check the connectivity
# VERSION

use strict;
use warnings;
use parent 'HealthCheck::Diagnostic';

1;
